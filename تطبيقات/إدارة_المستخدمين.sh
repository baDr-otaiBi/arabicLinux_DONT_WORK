#!/bin/bash
# ===================================================================
# برنامج إدارة المستخدمين - User Management Script
# ===================================================================
#
# الوصف: هذا البرنامج يوفر نظام بسيط لإدارة المستخدمين في التوزيعة
#         ويشمل عمليات إضافة وحذف وعرض معلومات المستخدمين
#
# Description: This script provides a simple user management system
#              including add, delete, and display user information
#
# الاستخدام: ./إدارة_المستخدمين.sh [الأمر] [اسم_المستخدم]
# Usage: ./إدارة_المستخدمين.sh [command] [username]
#
# ===================================================================

# ===================================================================
# الإعدادات - Settings
# ===================================================================

# مسار قاعدة بيانات المستخدمين
# Users database path
مسار_المستخدمين="$HOME/.مستخدمون_عرب"

# إنشاء المسار إذا لم يكن موجوداً
# Create path if it doesn't exist
mkdir -p "$مسار_المستخدمين"

# ===================================================================
# الدوال المساعدة - Helper Functions
# ===================================================================

# دالة عرض رسالة
# Display message function
عرض_رسالة() {
    local النوع="$1"
    local الرسالة="$2"
    
    case "$النوع" in
        "معلومات")
            echo "👤 [معلومات] $الرسالة"
            ;;
        "نجاح")
            echo "✅ [نجاح] $الرسالة"
            ;;
        "خطأ")
            echo "❌ [خطأ] $الرسالة"
            ;;
        "تحذير")
            echo "⚠️  [تحذير] $الرسالة"
            ;;
    esac
}

# دالة عرض المساعدة
# Display help function
عرض_المساعدة() {
    echo "═══════════════════════════════════════════════════════════"
    echo "  👥 برنامج إدارة المستخدمين - User Management Program"
    echo "═══════════════════════════════════════════════════════════"
    echo ""
    echo "الاستخدام (Usage):"
    echo "  $0 [الأمر] [اسم_المستخدم]"
    echo ""
    echo "الأوامر المتاحة (Available Commands):"
    echo ""
    echo "  إضافة | add [اسم_المستخدم]"
    echo "    - إضافة مستخدم جديد للنظام"
    echo "    - Add a new user to the system"
    echo ""
    echo "  حذف | delete [اسم_المستخدم]"
    echo "    - حذف مستخدم من النظام"
    echo "    - Delete a user from the system"
    echo ""
    echo "  معلومات | info [اسم_المستخدم]"
    echo "    - عرض معلومات المستخدم"
    echo "    - Display user information"
    echo ""
    echo "  قائمة | list"
    echo "    - عرض قائمة جميع المستخدمين"
    echo "    - Display list of all users"
    echo ""
    echo "  مساعدة | help"
    echo "    - عرض هذه الرسالة"
    echo "    - Display this message"
    echo ""
}

# ===================================================================
# دوال العمليات - Operation Functions
# ===================================================================

# دالة إضافة مستخدم
# Add user function
إضافة_مستخدم() {
    local اسم_المستخدم="$1"
    
    if [ -z "$اسم_المستخدم" ]; then
        عرض_رسالة "خطأ" "يجب تحديد اسم المستخدم"
        return 1
    fi
    
    # التحقق من وجود المستخدم
    # Check if user exists
    if [ -f "$مسار_المستخدمين/$اسم_المستخدم.conf" ]; then
        عرض_رسالة "خطأ" "المستخدم '$اسم_المستخدم' موجود بالفعل"
        return 1
    fi
    
    عرض_رسالة "معلومات" "جاري إضافة المستخدم: $اسم_المستخدم"
    
    # إنشاء ملف معلومات المستخدم
    # Create user information file
    cat > "$مسار_المستخدمين/$اسم_المستخدم.conf" << EOF
# ملف معلومات المستخدم - User Information File
# المستخدم: $اسم_المستخدم

اسم_المستخدم="$اسم_المستخدم"
تاريخ_الإنشاء="$(date '+%Y-%m-%d %H:%M:%S')"
المجموعة="مستخدمون"
الصلاحيات="عادي"
الحالة="نشط"

# نهاية الملف
EOF
    
    # إنشاء مجلد المستخدم
    # Create user directory
    mkdir -p "$مسار_المستخدمين/مجلدات/$اسم_المستخدم"
    
    عرض_رسالة "نجاح" "تم إضافة المستخدم '$اسم_المستخدم' بنجاح"
    echo "  📁 المجلد الشخصي: $مسار_المستخدمين/مجلدات/$اسم_المستخدم"
}

# دالة حذف مستخدم
# Delete user function
حذف_مستخدم() {
    local اسم_المستخدم="$1"
    
    if [ -z "$اسم_المستخدم" ]; then
        عرض_رسالة "خطأ" "يجب تحديد اسم المستخدم"
        return 1
    fi
    
    # التحقق من وجود المستخدم
    # Check if user exists
    if [ ! -f "$مسار_المستخدمين/$اسم_المستخدم.conf" ]; then
        عرض_رسالة "خطأ" "المستخدم '$اسم_المستخدم' غير موجود"
        return 1
    fi
    
    عرض_رسالة "معلومات" "جاري حذف المستخدم: $اسم_المستخدم"
    
    # حذف ملف المستخدم
    # Delete user file
    rm -f "$مسار_المستخدمين/$اسم_المستخدم.conf"
    
    # حذف مجلد المستخدم (اختياري)
    # Delete user directory (optional)
    if [ -d "$مسار_المستخدمين/مجلدات/$اسم_المستخدم" ]; then
        rm -rf "$مسار_المستخدمين/مجلدات/$اسم_المستخدم"
    fi
    
    عرض_رسالة "نجاح" "تم حذف المستخدم '$اسم_المستخدم' بنجاح"
}

# دالة عرض معلومات المستخدم
# Display user info function
عرض_معلومات_المستخدم() {
    local اسم_المستخدم="$1"
    
    if [ -z "$اسم_المستخدم" ]; then
        عرض_رسالة "خطأ" "يجب تحديد اسم المستخدم"
        return 1
    fi
    
    # التحقق من وجود المستخدم
    # Check if user exists
    if [ ! -f "$مسار_المستخدمين/$اسم_المستخدم.conf" ]; then
        عرض_رسالة "خطأ" "المستخدم '$اسم_المستخدم' غير موجود"
        return 1
    fi
    
    عرض_رسالة "معلومات" "معلومات المستخدم: $اسم_المستخدم"
    echo ""
    
    # تحميل معلومات المستخدم
    # Load user information
    source "$مسار_المستخدمين/$اسم_المستخدم.conf"
    
    echo "  👤 اسم المستخدم: $اسم_المستخدم"
    echo "  📅 تاريخ الإنشاء: $تاريخ_الإنشاء"
    echo "  👥 المجموعة: $المجموعة"
    echo "  🔐 الصلاحيات: $الصلاحيات"
    echo "  ✓ الحالة: $الحالة"
    echo ""
}

# دالة عرض قائمة المستخدمين
# List users function
عرض_قائمة_المستخدمين() {
    عرض_رسالة "معلومات" "قائمة المستخدمين في النظام:"
    echo ""
    
    local عدد=0
    
    # البحث عن ملفات المستخدمين
    # Search for user files
    for ملف in "$مسار_المستخدمين"/*.conf; do
        if [ -f "$ملف" ]; then
            source "$ملف"
            echo "  👤 $اسم_المستخدم - $المجموعة ($الحالة)"
            ((عدد++))
        fi
    done
    
    if [ $عدد -eq 0 ]; then
        echo "  لا يوجد مستخدمون في النظام"
    else
        echo ""
        echo "  إجمالي المستخدمين: $عدد"
    fi
    echo ""
}

# ===================================================================
# البرنامج الرئيسي - Main Program
# ===================================================================

البرنامج_الرئيسي() {
    local الأمر="$1"
    local المعامل="$2"
    
    case "$الأمر" in
        "إضافة"|"add")
            إضافة_مستخدم "$المعامل"
            ;;
        "حذف"|"delete")
            حذف_مستخدم "$المعامل"
            ;;
        "معلومات"|"info")
            عرض_معلومات_المستخدم "$المعامل"
            ;;
        "قائمة"|"list")
            عرض_قائمة_المستخدمين
            ;;
        "مساعدة"|"help"|"")
            عرض_المساعدة
            ;;
        *)
            عرض_رسالة "خطأ" "أمر غير معروف: $الأمر"
            echo ""
            عرض_المساعدة
            return 1
            ;;
    esac
}

# ===================================================================
# تشغيل البرنامج - Run Program
# ===================================================================

البرنامج_الرئيسي "$@"

# ===================================================================
# نهاية البرنامج - End of Script
# ===================================================================
